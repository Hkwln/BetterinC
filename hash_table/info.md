# Build a hash table:

     - Stores key-value pairs (string keys, integer values)
     - Uses separate chaining for collision resolution (linked lists)
     - Implements: hash_insert(), hash_get(), hash_delete(), hash_print()
     - Use a simple hash function (sum of ASCII values % TABLE_SIZE)
     - Handle memory allocation properly (malloc/free)
     - Test with at least 10 insertions including collisions


       ---CONSIDER AS FINISHED---
