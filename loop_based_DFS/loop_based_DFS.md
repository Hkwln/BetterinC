# Loop-Based Deep field search algorithm:

## features:

- Index-based design
- State-pushing approach:
  - marks the node visited when creating its new stack frame
- handles disconnected components

stack: dynamically resizable array

//currenty: does only supporte interleaved nodes and **not** orphaned once
